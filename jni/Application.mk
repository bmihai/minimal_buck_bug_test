# Minimum level of Android API Level support
APP_PLATFORM := android-11

# Build system architecture against which to build
APP_ABI := armeabi-v7a
#APP_ABI := all

#ndk-build compilation options
APP_CFLAGS += -Werror
